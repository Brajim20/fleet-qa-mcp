# build output
/build/

# per-user secrets & runtime state — NEVER commit these
.env
*.env
!.env.example
/.auth/
# self-provisioned Fleet checkout (managed clone)
/.fleet-src/
# browser screenshots captured during live investigations
/studio/.shots/

# OS / editor
.DS_Store
